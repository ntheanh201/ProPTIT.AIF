import React, { Component } from 'react';
import { ReactMic } from 'react-mic';
import Sound from 'react-sound';

export default class EveSound extends Component {
    render(){
        return (
            <div>fefeufhef</div>
        )
    }
}